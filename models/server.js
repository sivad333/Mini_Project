/*const dbConfig = require('../config/db.config');

const mongoose = require('mongoose');
mongoose.Promise = global.Promise;

module.exports = db = {};
db.mongoose = mongoose;
db.url = dbConfig.url;
db.details = require('./model.js')(mongoose);
*/

